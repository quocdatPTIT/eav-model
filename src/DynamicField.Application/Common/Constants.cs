namespace DynamicField.Common
{
    public static class Constants
    {
        public enum TicketValueType
        {
            INT,
            DECIMAL,
            DATETIME,
            VARCHAR,
            TEXT
        }
    }
}